---
scope: Hexalith.McpCli v1: Decoration Package, Catalog, executor, MCP Server head, CLI head, profiles, test fixture; HTTP transport seam only
purpose: build-substrate
altitude: feature
updated: 2026-09-22T10:50
---

- (event) run started; coaching path; spine-only deliverable; inputs: PRD prd-mcpcli-2026-09-21 (final), addendum, brief + brief addendum, extract-references
- (constraint) INHERITED D1 (brief 2026-09-21): hybrid discovery; Decoration Attribute carries description/example/name/routing fallbacks; ICommandContract/IQueryContract win for routing when implemented (PRD FR-1, FR-2)
- (constraint) INHERITED D2: compile-time package references + assembly-level Module marker; scan list generated at build time, never a folder scan, never hand-maintained (PRD FR-3, FR-5)
- (constraint) INHERITED D3: exactly five Generic Tools list_modules/list_operations/describe_operation/send_command/run_query; surface never changes with the Catalog (PRD FR-9); tool budget < 8000 chars (NFR-2)
- (constraint) INHERITED D4: one binary, 'mcp --transport stdio|http'; v1 stdio only; HTTP next release via forwarding DelegatingHandler pattern; no auth server ever in scope (PRD 8.2, 8.3)
- (constraint) INHERITED D5: thin CLI verbs modules/operations/describe/send/query/mcp/config; global --url --token --tenant --profile --format --output --read-only --strict; resolution order per-call arg > flag > env > Profile > default; exit codes 0/1/2 (PRD FR-12..14, addendum E)
- (constraint) INHERITED D6: replace legacy servers; migration plan is a v1 deliverable but not code in this repo (PRD FR-21, FR-22)
- (constraint) INHERITED guardrails (PRD 4): only EventStore client package + *.Contracts packages as Hexalith deps; zero module-specific code; Tenant envelope-only never from Payload; ULIDs via Ulid.TryParse; one message id + one idempotency key per call, never retry; read-only enforced in executor; no hosted infra in v1
- (constraint) INHERITED stack (PRD addendum B): .NET 10, C# 14, .slnx only, central package management via Hexalith.Builds, ModelContextProtocol 2.2.0, System.CommandLine 2.0.12, xUnit v3 + Shouldly + NSubstitute, TreatWarningsAsErrors, no Spectre.Console, flat layout src/Hexalith.McpCli.* and tests/Hexalith.McpCli.*.Tests
- (constraint) INHERITED channels (PRD FR-10, NFR-4): stdout is JSON-RPC (mcp) or the result document (cli); all logs to stderr via source-generated LoggerMessage; tokens never on stdout/stderr (FR-18)
- (constraint) INHERITED artifacts (PRD 7): three public surfaces: Decoration Package (zero package refs), tool package (CLI + MCP Server, dotnet global tool), Generic Tool contract; Decoration Package published first
- (constraint) INHERITED config (PRD FR-18): ~/.eventstore/profiles.json shared read-only with admin CLI; ~/.eventstore/mcpcli.json for per-Profile default tenant; env vars EVENTSTORE_URL/TOKEN/TENANT/FORMAT/READ_ONLY/PROFILE
- (question) OPEN (PRD OQ-7): package identifiers and CLI command name; brief uses 'hexalith'; needed before scaffolding
- (question) OPEN (PRD OQ-3): aggregate identifier the Gateway expects for list-style Queries; EventStore owner; before FR-16
- (question) OPEN (PRD NFR-7): integration test harness choice (Aspire vs container vs external URL) is the architecture's call
- (question) OPEN (PRD addendum E): default Gateway URL is the architecture's call
- (decision) AD paradigm: hexagonal, heads as adapters. Binds all. Core (Catalog + Executor + settings resolution) has no reference to ModelContextProtocol, System.CommandLine, or ASP.NET; heads translate tool args / verbs into one shared request model and back. Ports: IEventStoreGatewayClient, settings sources, clock/id generation. Prevents: a head validating, filling envelopes, or enforcing read-only on its own; heads disagreeing (SM-4). Alternatives shown: layered, single pipeline service. User chose hexagonal.
- (decision) AD project split: four src projects + fixture. Hexalith.McpCli.Abstractions (Decoration Package, zero package refs) | Hexalith.McpCli.Core (catalog, schema, executor, settings; refs EventStore.Client + Abstractions) | Hexalith.McpCli.Mcp (MCP head; refs Core + ModelContextProtocol) | Hexalith.McpCli (tool exe: CLI head, composition root, 'mcp' verb, the ONLY project referencing *.Contracts packages, PackAsTool) | Hexalith.McpCli.Analyzers optional separable. tests/Hexalith.McpCli.Sample.Contracts fixture + one test project per src project. Prevents: Contracts refs leaking into a library, two composition roots, heads sharing an assembly with core. Alternatives shown: three, two. User chose four.
- (decision) AD naming (closes PRD OQ-7): CLI command 'hexalith'; tool PackageId Hexalith.McpCli; Decoration PackageId Hexalith.McpCli.Abstractions. Alternatives shown: hexalith-mcp, mcpcli. User chose hexalith.
- (decision) AD shared call model: Core owns one request record (operation name, payload JsonElement, envelope arguments: tenant?, aggregateId?, correlationId?, idempotencyKey?, entityId?, paging?, extensions?), one CommandResult, one QueryResult, one OperationError (code + details) record set; both heads serialize exactly these records with Core's canonical JsonSerializerOptions; MCP structuredContent and CLI stdout document are byte-identical for the same call (PRD SM-4, FR-11). Prevents: two result dialects.
- (decision) AD catalog is the only view of Contracts types: Catalog builds once at startup into an immutable model (Module, Operation with kind/name/description/example/schema/resolved routing/aggregate-id JSON path/readOnly property names) plus a diagnostics list; heads and executor consume the model only; System.Reflection appears only in Core's catalog builder. Prevents: a head or the executor re-reflecting types and disagreeing with the catalog.
- (decision) AD transport seam: executor receives an EnvelopeContext value (tenant, correlation id, optional user id, extensions) built by the head per call from settings/args; authentication is a DelegatingHandler on the gateway HttpClient registered by the composition root (v1: static bearer token from settings; HTTP release: forwarding handler per Parties pattern). Executor and Core never see tokens, headers, or transports. Prevents: the HTTP release touching Core.
- (decision) AD profile files: this tool reads and writes ~/.eventstore/profiles.json with the admin CLI's exact schema {version:1, activeProfile, profiles:{name:{url,token,format}}} and adds no field to it; a round-trip test against a fixture written by the admin CLI guards it; every tool-specific setting (default tenant) lives in ~/.eventstore/mcpcli.json keyed by profile name. Prevents: this tool corrupting or being corrupted by the admin CLI's rewrite.
- (decision) AD read-only enforcement: single ReadOnly flag in settings; the executor refuses kind=write with code read_only; the MCP head's list-tools handler omits send_command when set; describe reports submittable=false reason=read_only. Adopted from PRD FR-19.
- (decision) AD output formatting: table rendering exists only in the CLI head; Core and the MCP head emit JSON only; --output writes the same document to a file. Prevents: formatting logic in Core.
- (direction) Deferred candidates so far: HTTP transport auth design (OQ-1); typed tools behind module filter; per-operation subcommands; analyzer beyond missing-description rule; command status lookup; query search/filter/order/freshness
- (version) Verified 2026-09-22 (web + Hexalith.Builds catalog): .NET SDK 10.0.401 (global.json, MTP runner); ModelContextProtocol/.Core/.AspNetCore 2.2.0 (2026-08-13, implements MCP 2026-07-28, compatible with 2025-11-25); System.CommandLine 2.0.12 (2026-09-08; 3.0 rc tracks .NET 11); xunit.v3 4.0.1; Shouldly 4.3.0; NSubstitute 6.2.0; Verify.XunitV3 33.x (Verify.Xunit deprecated); Aspire.Hosting(.Testing) 13.5.4; CommunityToolkit.Aspire.Hosting.Dapr 13.5.1-beta (Aspire.Hosting.Dapr deprecated); ByteAether.Ulid 1.4.1 (catalog pin, wrapped by Hexalith.Commons.UniqueIds); Microsoft.CodeAnalysis.* 5.9.0; Microsoft.Extensions.* 10.0.12; JsonSchema.Net 9.4.0 (2026-07-26, NOT in the Hexalith.Builds catalog); Microsoft.Extensions.AI.Abstractions 10.10.0 (transitive via MCP Core, not catalogued)
- (version) Verified API facts: McpServerTool.Create(Delegate|MethodInfo|AIFunction, McpServerToolCreateOptions{ReadOnly, Destructive, Idempotent, UseStructuredContent, OutputSchema}); WithListToolsHandler/WithCallToolHandler; StdioServerTransport does not guard stdout; STJ JsonSchemaExporter targets 2020-12, emits no description/readOnly natively, additionalProperties:false via JsonUnmappedMemberHandling.Disallow, everything else via TransformSchemaNode; AIJsonUtilities.CreateJsonSchema emits [Description] natively; JsonSchema.Net evaluates JsonNode with InstanceLocation pointers, AOT-compatible; System.CommandLine 2.0: Option.Recursive, SetAction, ParseResult.InvokeAsync(InvocationConfiguration)
- (constraint) REALITY (EventStore code): IEventStoreGatewayClient.SubmitCommandAsync returns SubmitCommandResponse(CorrelationId, ResultPayload?, MessageId?); SubmitQueryAsync returns EventStoreQueryResult(CorrelationId?, Payload?, IsNotModified, ETag) + Metadata.Paging (QueryPagingMetadata PageSize/Offset/NextCursor/TotalCount/HasMore); AddEventStoreGatewayClient returns IHttpClientBuilder for handler chaining; client ships no auth handler; EventStoreGatewayClientOptions.BaseAddress default null; EventStore dev launch URL http://localhost:8080
- (constraint) REALITY (EventStore serialization): gateway passes Payload bytes through untouched; aggregates bind with EventStorePayloadSerialization.Options (Web defaults, case-insensitive) but Tenants' TenantSubmitCommandValidator deserializes with default options (case-sensitive PascalCase); EventStore doc: writers keep PascalCase. Aggregate discriminator table accepts both CLR short name and ICommandContract.CommandType. ICommandContract = static CommandType/Domain + instance AggregateId; IQueryContract = static QueryType/Domain/ProjectionType only. All wire ids are strings.
- (constraint) REALITY (sibling ops): CI = thin ci.yml calling Hexalith/Hexalith.Builds/.github/workflows/domain-ci.yml@main (ubuntu-latest only, SDK from global.json, unit / integration / aspire-test-project tiers, dapr-init for tiers 2-3, coverage gate); release = workflow_dispatch release.yml + semantic-release (.releaserc.json, tagFormat v${version}, 'dotnet build <slnx> -p:Version=${nextRelease.version}', pack-release-packages.py over tools/release-packages.json, push to nuget.org); no MinVer; dev version from Hexalith<Module>Version property in Hexalith.Builds catalog; Directory.Build.props imports only Props/Directory.Packages.props from references/Hexalith.Builds with 3-path fallback; tests/Directory.Build.props injects xunit.v3/Shouldly/NSubstitute/coverage
- (decision) AD assembly enumeration: Roslyn incremental generator project Hexalith.McpCli.Generators (netstandard2.0, IsRoslynComponent, EnforceExtendedAnalyzerRules, consumed by the tool exe as ProjectReference OutputItemType=Analyzer ReferenceOutputAssembly=false; never packed) walks compilation.SourceModule.ReferencedAssemblySymbols for the assembly-level Module marker and emits a static manifest of typeof(<first decorated type>).Assembly per module, ordinally sorted. Ratifies Hexalith.EventStore.RestApi.Generators pattern. Alternatives shown: MSBuild target, runtime deps.json scan. User chose generator.
- (decision) AD integration harness: tests/Hexalith.McpCli.AppHost (Aspire.AppHost.Sdk 13.5.x) composes the EventStore platform through Hexalith.EventStore.Aspire plus the Tenants domain service; tests/Hexalith.McpCli.IntegrationTests subclasses AspireTopologyFixtureBase from Hexalith.EventStore.Testing.Integration, drives BOTH heads (CLI process + MCP stdio client) against the same topology for SM-4, and skips via the standard Dapr prerequisite check; wired to domain-ci.yml aspire-test-project. Alternatives shown: external URL, fakes only. User chose AppHost.
- (decision) AD test dependency policy: test projects may additionally reference Hexalith.EventStore.Testing, Hexalith.EventStore.Testing.Integration, Hexalith.EventStore.Aspire, Aspire.Hosting.Testing, and the Module Aspire composition helpers needed to run the topology; never a Module aggregate/projection/handler/client/server package; the tool package closure stays client + Contracts. Alternatives shown: strict, anything. User chose Testing/Aspire packages in tests only.
- (decision) AD ULID: Core references ByteAether.Ulid (catalog pin 1.4.1) directly; Ulid.New() for message ids and idempotency keys, Ulid.TryParse for validation; ids travel as strings. Adopted from ecosystem (Hexalith.Commons.UniqueIds wraps the same library). Not asked: routine.
- (decision) AD wire payload naming: the Schema property names and the submitted Payload property names are the CLR property names verbatim (PropertyNamingPolicy null), matching EventStore writers and Tenants' case-sensitive validator; enum values serialize as names (JsonStringEnumConverter); nulls omitted. Core's canonical options for RESULT documents (what heads emit) are camelCase Web defaults. Two options objects, both owned by Core, never constructed elsewhere. Prevents: a camelCase payload silently binding to nulls in a module validator.
- (decision) AD schema: Core derives each Operation Schema with System.Text.Json JsonSchemaExporter (2020-12) using the payload options plus one TransformSchemaNode that adds description from [Description], readOnly for envelope-filled properties, the ULID pattern for identifiers, and additionalProperties:false; validation with JsonSchema.Net (List output, InstanceLocation as JSON path). New central pin JsonSchema.Net required in Hexalith.Builds. Alternatives shown: AIJsonUtilities, Corvus. User chose STJ + JsonSchema.Net.
- (decision) AD default URL: http://localhost:8080 when no --url, EVENTSTORE_URL, or Profile url [ASSUMPTION: EventStore host launch profile; Aspire users use a Profile]. Alternative shown: no default. User chose 8080.
- (decision) AD analyzer packaging: Hexalith.McpCli.Analyzers (netstandard2.0, Microsoft.CodeAnalysis.CSharp PrivateAssets=all) is packed into the Hexalith.McpCli.Abstractions package under analyzers/dotnet/cs; not a package reference, so the zero-dependency rule holds; Abstractions may ship before the analyzer exists. Alternatives shown: separate package, defer. User chose inside.
- (decision) AD release: ratify sibling tooling. semantic-release on main, one version for the whole solution (-p:Version), tools/release-packages.json lists Hexalith.McpCli.Abstractions and Hexalith.McpCli, push to nuget.org; CI via Hexalith.Builds domain-ci.yml with aspire-test-project; both packages share one semantic version; PRD section 7 breaking-change rules drive the major. HexalithMcpCliVersion property added to Hexalith.Builds catalog upstream. Alternative shown: independent versions. User chose lockstep.
- (decision) AD tool packaging: framework-dependent dotnet global tool, untrimmed, CreateRidSpecificToolPackages=false and UseAppHost=false (reflection scanning of Contracts assemblies; NFR-6 no native deps); PackAsTool, ToolCommandName hexalith. Routine, from web facts on .NET 10 RID-specific tool packages.
- (decision) AD composition root: the tool exe owns the single DI container (Host.CreateApplicationBuilder) for every verb; Core exposes AddMcpCliCore(settings) registering catalog, executor, gateway client (AddEventStoreGatewayClient + auth handler); the Mcp project exposes AddMcpCliMcpServer() registering the five McpServerTool instances via McpServerTool.Create with UseStructuredContent + OutputSchema and a list-tools handler that applies read-only; CLI verbs resolve services from the same container. Prevents: two wiring paths, admin-CLI-style no-DI drift in one head.
- (question) CONFLICT for user: PRD FR-7 identifier heuristic (name ends in Id => ULID pattern) breaks Tenants: TenantIdentity.DefaultTenantId = 'system' and CreateTenant.TenantId / aggregate id are tenant ids, not ULIDs
- (decision) AD identifiers (amends PRD FR-7): only a property marked by the Decoration Package's identifier attribute, or the aggregateIdProperty of a Module whose marker declares ULID ids, gets the ULID pattern and Ulid.TryParse validation; an unmarked *Id property is a plain string plus a describe --lint 'unmarked identifier' warning. Message id, idempotency key, correlation id remain ULID-validated (FR-16). PRD update note required. Alternatives shown: opt-out marker, keep FR-7. User chose explicit marking.
- (event) spine distilled from memlog (18 ADs); reviewer gate started
- (constraint) REALITY (Aspire helpers): Hexalith.EventStore.Aspire and Hexalith.Tenants.Aspire compose sibling services with AddProject<IProjectMetadata> whose ProjectPath is resolved at runtime by RepositoryProjectPaths.GetReferencedModuleProjectPath (candidates include <repo>/references/<Module>/src/...), SuppressBuild=true; the *.Aspire packages depend only on Aspire.Hosting (+ EventStore.Aspire), never on server code. So a test AppHost referencing Hexalith.Tenants.Aspire adds no Module server package; the Tenants server runs from the references/ submodule checkout and must be built beforehand.
- (decision) AD-15/AD-16 tightening (gate finding brief-C1, prd-H1): test AppHost may reference Hexalith.<Module>.Aspire composition packages only; servers are composed from references/ submodule source at run time; topology includes every Module v1 must cover end to end (Tenants and Parties, FR-20); CI builds the referenced sources before the aspire tier (open question: exact domain-ci.yml inputs).
- (direction) Gate fixes queued from prd/brief reconcile: AD-9 add FR-16 disagreement checks (payload tenant vs envelope tenant; explicit aggregateId vs payload) => validation_failed; AD-13 mcp verb accepts all global options + --transport, settings resolved once at startup, tool args fill per-call slot; AD-4 manifest emits assembly names + Assembly.Load so marker-only assemblies (Folders today) appear with zero Operations; AD-8 state full amendment scope (known identifier CLR type Ulid keeps pattern; value-object identifiers follow their declared converter, tool adds none; marker gains IdentifierKind; glossary 'Identifiers are ULIDs' narrows to generated ids); AD-5 add discovery shapes ModuleSummary/OperationSummary/OperationDescription(Submittable, SubmittableReason), OperationError.Suggestions, Payload accepted as text and parsed in Core (non-JSON = one violation); EnvelopeContext (session) vs EnvelopeArguments (per call) precedence; Stack add Projects/Folders Contracts 1.0.0 and ModelContextProtocol.AspNetCore 2.2.0; conventions: no Spectre.Console, list six EVENTSTORE_* names, lint rules undescribed_property/unmarked_identifier/hollow_description (NFR-8); Open Questions: record OQ-2/OQ-4/OQ-7 as resolved here.
- (constraint) REALITY (nuget 2026-09-20): Hexalith.Projects.Contracts 1.0.0 carries FrameworkReference Microsoft.AspNetCore.App, Fluxor, FluentUI, Hexalith.FrontComposer.Shell, OpenIdConnect, NUlid + ByteAether.Ulid. Referencing it violates PRD guardrail 4 (module project in closure) and NFR-6.
- (decision) AD-15 tightening (gate finding tech-C1, amends PRD FR-20): the exe references a *.Contracts package only when its transitive closure contains no Module project other than Contracts packages and no FrameworkReference beyond Microsoft.NETCore.App; a closure test over the restored assets file enforces it in CI. v1 references Hexalith.Tenants.Contracts and Hexalith.Parties.Contracts; Projects and Folders Contracts are added when their packages satisfy the closure rule (a Gateway-ready prerequisite recorded for their maintainers). PRD update note.
- (direction) Gate fixes queued from tech-currency: Stack lists ModelContextProtocol 2.2.0 only (Core transitive; .Core not catalogued); Core references Microsoft.Extensions.Logging.Abstractions (console provider via Hosting in the exe); Aspire.AppHost.Sdk pinned 13.5.4; AD-12: list-tools handler emits full Tool records incl. inputSchema, read-only omission is process-wide because settings resolve at startup (spec forbids per-connection variation); AD-7: InstanceLocation is a JSON Pointer, rendered as the violation path.
- (direction) Gate fixes queued from rubric: AD-3 CatalogBuilder takes IReadOnlyList<Assembly> (manifest in the exe, Sample.Contracts in tests); Catalog is built lazily by the first verb that needs it, so config/--version/mcp --transport http never touch it; empty Catalog = exit 2 for catalog verbs and mcp only; Stack rows for Contracts packages read 'first decorated release (>= current pin)'; AD-9/AD-10 executor alone merges Arguments.Tenant ?? Context.Tenant (same for CorrelationId, Extensions merged per key with argument winning); EnvelopeContext v1 = settings Tenant only, UserId arrives with the HTTP release; AD-5/AD-12 set McpServerToolCreateOptions.SerializerOptions = McpCliJson.Result and define SM-4 equality as JsonElement.DeepEquals; error codes: configuration_missing for unresolved Url/Token, internal_error for escaped exceptions; AD-15/16: Tenants and Parties servers composed from references/ submodule source at run time (decided, not open); CI checks out top-level submodules and builds those sources before the aspire tier.
- (decision) AD-19 aggregate identifier resolution (gate adversarial-C2/C3): OperationDescriptor.AggregateIdAccessor Func<JsonElement,string?> compiled at Catalog build: JSON Pointer read when aggregateIdProperty is set, else deserialize validated Payload into the ICommandContract type with McpCliJson.Payload and read the getter; Command with neither excluded (missing_routing_values); aggregateIdProperty optional for a Query (amends PRD 5.1); executor: explicit arg else accessor; Command with none validation_failed; Query with none sends empty string [ASSUMPTION OQ-3]. Rejected reviewer alternative: require aggregateIdProperty on every Operation (would break the PRD FR-2 promise for Tenants).
- (decision) AD-20 paging Envelope-only (gate adversarial-H4): pageSize/offset/cursor only as RunQueryArguments -> SubmitQueryRequest.Paging; Payload members so named are ordinary Payload + lint unmarked_paging_member; attribute paging members deferred.
- (decision) Gate tightenings applied: AD-3 Build(IReadOnlyList<Assembly>), lazy Catalog, catalog_empty only for catalog verbs, ICatalog.Describe computes Submittable; AD-4 manifest = assembly names + Assembly.Load, no_operations diagnostic for marker-only assemblies; AD-5 argument + discovery + error records, object-only documents, Payload parsed in Core, DeepEquals equality; AD-7 remove get-only members, EnvelopeFilledProperties explicit set; AD-8 IdentifierKind on marker, one validation rule in executor, tenant never ULID-validated; AD-9 FR-16 disagreement checks, Query correlationId/extensions via AdditionalProperties [ASSUMPTION], internal_error; AD-10 context = session values only, executor merges, auth handler only in Hosting via callback; AD-11 resolver once per process; AD-12 SerializerOptions=Result, OutputSchema object + optional error, IsError carriage, full Tool records; AD-13 mcp accepts all global options + --transport, EVENTSTORE_STRICT, configuration_missing; AD-14 reserved names, orphaned_tenant; AD-15 closure rule (Projects.Contracts excluded until slimmed), *.Aspire packages allowed in tests; AD-16 Tenants + Parties, out-of-process both heads, submodule build in CI; Stack: ModelContextProtocol only, Logging.Abstractions in Core, AppHost SDK 13.5.4, Contracts pins 'first decorated release'; conventions: kebab-case algorithm, lint list, error codes, no Spectre, six+one env vars, Sample.Contracts two modules; PRD Amendments section added.
- (direction) Triage of [ASSUMPTION] tags at close: default URL 8080 (revisit if operators report misdirected calls); OQ-3 empty aggregate id for Queries and Query correlationId/extensions via AdditionalProperties (revisit before FR-16 story; EventStore owner). None blocks epics.
- (direction) Reviewer alternatives NOT taken: adversarial-M5 (warnings exempt from exit 1) rejected, PRD FR-14 kept; adversarial-H6 (typeof first public type + MCPCLI001) replaced by assembly-name manifest; adversarial-H5 strict rejection of Query correlationId replaced by forwarding under ASSUMPTION.
- (event) spine finalized
- (event) update resumed from finalized spine; authoritative input is PRD revision 2026-09-22 section 10.1; scope is pre-executor reconciliation of AD-7, AD-9, AD-11, AD-15, and AD-19
- (constraint) PRD handoff names AD-11 for profile storage, while the stable rendered spine places the profile-file decision in AD-14. Preserve IDs: amend AD-11 settings bootstrap and AD-14 profile storage together; do not repurpose or renumber either decision.
- (decision) AD-7 amendment [ADOPTED from PRD FR-7]: EnvelopeFilledProperties includes actorProperty in addition to tenantProperty, correlationProperty, and idempotencyKeyProperty; Schema marks all as readOnly and removes them from required.
- (decision) AD-9 amendment [ADOPTED from PRD FR-16]: tenant precedence is fixedTenant, then gated per-call MCP tenant, then session tenant; per-call tenant is accepted only when session tenant is absent or AllowTenantOverride is true, and disagreements fail validation. Actor is session-only and required for actorProperty. Extensions are Command-only and limited by AllowedExtensions. Queries carry neither correlation nor extensions.
- (decision) AD-11 and AD-14 amendment [ADOPTED from PRD FR-13 and FR-18]: one ProfileStore reads and writes only ~/.eventstore/mcpcli.json. ResolvedSettings adds Actor, AllowTenantOverride, and AllowedExtensions. The separate admin profiles store and McpCliSettingsStore are removed; URL has no default.
- (decision) AD-15 amendment [ADOPTED from PRD section 4]: the production allowlist is exactly EventStore.Client, exposed Module Contracts packages, and the pinned EventStore.Contracts transitive closure, currently Hexalith.Commons.UniqueIds and ByteAether.Ulid. Any added package requires a PRD change; Contracts closures may add nothing outside that exact set or a framework reference beyond Microsoft.NETCore.App.
- (decision) AD-19 amendment [ADOPTED from PRD FR-2 and FR-16]: a Query aggregate identifier resolves explicit argument, then property accessor, then aggregateId constant; explicit wins over a constant but must agree with an accessor. A Query with no property or constant stays discoverable and requires an explicit argument. Nothing sends an empty aggregate identifier.
- (direction) Re-distillation must also remove stale derived claims: Query correlation and extensions from AD-5 and diagrams; shared profiles.json and two-file state; localhost URL default; OQ-3 and AdditionalProperties assumptions; and amendment text that still says a Query property is simply optional without documenting constant or required argument.
- (decision) Reconcile C1 [ADOPTED from PRD section 5.1 and FR-7]: ModuleDescriptor retains FixedTenant, WireTypeConvention, and cached ModulePayloadOptions. McpCliJson derives per-Module Payload options by placing provider converters before canonical converters; non-converter policy and all Envelope/result serialization remain tool-owned.
- (decision) Reconcile C2 [ADOPTED from PRD FR-15]: Catalog validation applies Gateway patterns to Routing Values and aggregateId constants; executor validation applies them before submission to tenant, resolved aggregateId, entityId, and extension keys, reporting the FR-15 JSON pointers.
- (decision) Reconcile H1 [ADOPTED from PRD FR-13 and addendum E]: only the MCP adapter binds Tenant in per-call argument records. CLI leaves it null and carries global --tenant through ResolvedSettings and EnvelopeContext; a surface-map test replaces the blanket one-option-per-record-property assertion.
- (decision) AD-4 amended [ADOPTED from PRD FR-5 and addendum B]: an MSBuild target derives the manifest from PackageReference items marked HexalithContracts=true after ResolvePackageAssets. The Roslyn manifest generator project is removed; the generated set must equal the flagged set.
- (question) OPEN prerequisite (PRD OQ-8 / FR-20): exact Tenants.Contracts and Parties.Contracts pins are selected when their first decorated releases are published; no range is permitted in implementation or release assets.
- (decision) Gate fix rubric-H1: Command correlation resolves once as caller value or generated MessageId; the resolved value is passed explicitly to SubmitCommandRequest and written to correlationProperty.
- (decision) Gate fix rubric-H2/M1: AD-15 is explicitly the Hexalith production dependency allowlist; external dependencies are limited by the pinned Stack. Tenant, Actor, and aggregate disagreements use ordinal equality. AllowedExtensions is ordinal-ignore-case and case-insensitive duplicates make configuration invalid.
- (decision) Gate fix tech-H1/M1: executor prevalidates the pinned Gateway extension limits (50 entries, key 100 chars, value 1000 chars, total 65536 UTF-8 bytes, dangerous/injection content rejected); the manifest target runs after ResolveReferences.
- (decision) Gate fix adversarial-C1: executor validates the rebuilt Payload again after inserting envelope-filled properties and before constructing a Gateway request; any violation returns validation_failed with zero gateway calls.
- (decision) Gate fix adversarial-H1: module marker SerializerOptionsProvider is a Type in the Contracts assembly exposing public static JsonSerializerOptions Options; Catalog validates and invokes it once, then copies its converters into immutable ModulePayloadOptions.
- (decision) Gate fix adversarial-M1: manifest tests include both flagged-unmarked and flagged-marked-empty assemblies, asserting invisible/no-diagnostic versus visible/empty_module behavior.
- (event) update reviewer gate passed: PRD reconciliation complete; lint_spine zero findings; rubric, technology/reality, and adversarial fallback reviews resolved; first decorated Contracts pins remain an explicit OQ-8 prerequisite
- (event) spine finalized after PRD section 10.1 reconciliation; executor epic may proceed under the updated AD set
