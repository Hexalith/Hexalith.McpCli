---
topic: Hexalith MCP server + CLI exposing CQRS commands/queries via EventStore client
updated: 2026-09-21T17:13
---

- (event) Create intent detected from skill arguments (goal + 3 requirements + decoupling constraints); workspace seeded
- (decision) Hard constraint from user: depend only on EventStore client package + domain modules' client libraries (command/query definitions only); no aggregates/projections/server code; stay pragmatic, no over-engineering
- (decision) Requirement: this module owns the decoration attributes that domain modules apply to commands/queries; MCP server and CLI discover operations via those attributes
- (decision) User chose Coaching path: section-by-section, push back on thin assumptions
- (decision) Problem framing: no triggering incident; anticipatory build ('I just see it coming'). Brief must state the anticipated pain explicitly rather than fabricate a current one
- (event) Web research digest (MCP C# SDK 2.2.0, dynamic tool registration, tool-count limits 30-50, azmcp/scw CLI+MCP precedents) captured to addendum.md
- (event) Codebase digest captured to addendum: 6 per-module hand-written .Mcp/.Cli projects already exist; definitions live in *.Contracts (not *.Client); two command styles (ICommandContract vs plain records); no public runtime catalog; EventStore client carries no auth (DelegatingHandler pattern)
- (event) Options-considered matrix (D1 discovery, D2 loading, D3 tool shape, D4 transport/identity, D5 CLI depth, D6 relation to existing per-module MCP servers) written to addendum with recommendations; awaiting user decisions
- (decision) D1 accepted: hybrid discovery - attribute package owned by this module carries LLM-facing metadata (description, kind, example); routing from ICommandContract/IQueryContract when implemented, attribute overrides otherwise; [Description] on properties
- (decision) D2 accepted: compile-time references to *.Contracts packages + assembly-level module marker; plugin loading parked
- (decision) D3 accepted: v1 exposes generic discovery tools (list modules, list operations, describe, send command, run query); typed per-operation tools with module filter deferred to follow-up
- (decision) D4 accepted: single binary with stdio and http transports; v1 ships stdio with profile/env token + default tenant; http mode with bearer/tenant/user header forwarding in v1.1, no new auth server
- (decision) D5 accepted: thin CLI (modules, operations, describe, send, query, mcp serve) with global url/token/tenant/format/profile flags, JSON on stdout, logs on stderr; generated per-operation subcommands deferred
- (override) D6 user overruled strangler recommendation: REPLACE the six existing per-module MCP servers (Parties, Projects, Folders, Memories, ChatBot, FrontComposer). Reason: user wants the inconsistency removed for good. Implication: generic server must reach feature parity with ChatBot catalog (read/write flag, correlation/task/tenant args); phasing of the migration still to be decided
- (decision) D6 cadence: replacement is the declared end state with cutover after v1; v1 proves the generic server on Tenants, Parties, Projects, Folders; rule 'no new per-module MCP server' applies from day one; each existing server is deleted once covered
- (decision) First consumer v1: a developer or an LLM agent such as Claude Code running the tool locally over stdio against a dev/test EventStore
- (decision) Primary cost driving the build: inconsistency an LLM sees across the six per-module MCP servers (different tool naming, args, descriptions, auth); maintenance and next-module friction are secondary
- (change) Drafted Executive Summary, The Problem, The Solution in brief.md for user review
- (decision) Vision (user's words): the MCP/CLI enables a human to do any operation in Hexalith through an LLM agent. Implication noted: identity forwarding (HTTP mode) becomes essential, not optional, and undecorated commands are invisible to agents
- (change) Drafted Who This Serves, Success Criteria (marked ASSUMPTION), Scope (in/next/out/guardrails), Vision in brief.md; brief now complete in draft form pending user checks
- (decision) User confirmed: belief behind anticipatory build = Hexalith Agents/ChatBot/Conversations will need to act across modules; 'no new per-module MCP server' is a rule the user is making, not proposed; five success criteria accepted; scope in/next/out accepted; vision policy sentence kept
- (change) Removed ASSUMPTION tags from brief.md after user confirmation; entering Finalize
- (change) Applied structure-lens findings to brief.md: vision sentence now opens the summary, Problem leads with thesis and bold lead-ins, HTTP plan stated once (Scope) with rationale in Vision, tenant rule merged into guardrails, routing detail deferred to addendum D1; ~1359 -> ~1150 words. Declined: dropping frontmatter title, retitling Vision
- (change) Applied structure-lens findings to addendum.md: added Decisions table reflecting actual outcomes (D6=Replace), moved cross-cutting rules to Constraints section before options, one schema per option with disposition tags, fixed 'five decisions' and MCP project count, replaced dangling 'requirement 2' reference, added Open items list, relevance notes on Comparables, dropped contributor-process rules
- (change) Applied prose-lens findings to brief.md: serial commas, disambiguated 'this module', 'EventStore gateway client' at first mention, 'in the second release', 'but the contract cannot express', .NET tool, handwritten, 'Out of version one' lead-in, comprehensibility wording of success criterion 4
- (change) Applied prose-lens findings to addendum.md: 'the user' -> 'the product owner' for the decision-maker, table column 'Not chosen', full Admin.Cli identifiers, ID capitalisation, serial commas, one term 'read/write kind', antecedent and modifier fixes, D6 glob scoped to the six domain projects
- (event) Finalize: both docs polished through structure+prose lenses; status set to ready-for-review; no external handoffs configured
