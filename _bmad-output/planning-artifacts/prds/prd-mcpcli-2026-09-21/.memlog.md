---
topic: Hexalith.McpCli PRD
updated: 2026-09-22T09:45
---

- (event) Create intent: workspace bound; prior input found at _bmad-output/planning-artifacts/briefs/brief-mcpcli-2026-09-21 (brief.md, addendum.md, .memlog.md)
- (event) Brain dump: user said 'continue' with no new context; brief bundle is the sole user-supplied input. Extraction, reference ground-truth, and landscape research subagents spawned.
- (decision) Stakes calibrated: Internal (platform module consumed by Hexalith modules and agents); target 5-8 pages with cross-cutting NFRs and developer-product clusters.
- (decision) Working mode: Fast path. Reason: user answered brain dump with 'continue' and confirmed Fast; remaining gaps batched into one consolidated question, then full draft with [ASSUMPTION] tags.
- (change) addendum.md seeded: rejected-alternatives summary (pointer to brief addendum), mechanism/transport decisions, research pointers, prior-art deletion targets.
- (assumption) Brief memlog line 7 says allowed dependency is modules' 'client libraries'; brief.md, its addendum, and CLAUDE.md all say *.Contracts. Treating *.Contracts as authoritative; *.Client projects hold HTTP senders, not definitions. To confirm with user.
- (decision) Section plan (Internal stakes, developer product): Essential Spine minus standalone personas; UJs kept light (three one-paragraph journeys: agent operator, LLM agent session, module author); add Cross-Cutting NFRs, Public Surface and Compatibility, Versioning and Dependency Policy, Migration of legacy servers (invented section for D6), Constraints and Guardrails (tenant, idempotency, read-only).
- (event) Landscape research written to research-landscape.md. Key facts: meta-tool catalogs converge on 4-6 tools (list/describe/invoke naming); MCP tool annotations (readOnlyHint etc.) are per-tool and treated as untrusted by clients, so read-only mode must be a catalog filter plus separate query tool, not an annotation on a shared invoke tool; generic-tools surface never changes so no listChanged needed; comparable read-only impl gates writes by env flag and auto-generates idempotency key.
- (assumption) Because send_command and run_query are separate tools, run_query can carry readOnlyHint=true and send_command destructiveHint per catalog; a --read-only flag removes send_command from the tool list entirely. To confirm with user in consolidated question.
- (event) extract-references.md written from Explore report. Surprises: only Parties.Mcp reaches the gateway; Folders.Contracts has no command/query records (OpenAPI only); Folders/Projects/ChatBot/Memories servers front module REST/Dapr with module-specific semantics; CommandType/Domain naming not uniform; Tenants is the only ICommandContract implementer; zero [Description] attributes anywhere; 4-5 sibling CLIs with divergent flags.
- (decision) Folders stays in v1 with a hard upstream dependency: Folders must publish decorated command/query records routed through the EventStore gateway (work in the Folders repo). Reason: user chose to keep the v1 module list rather than swap or narrow.
- (decision) Replace-all end state kept; parity defined as 'module exposes its agent-facing operations as gateway commands/queries'. Each REST/Dapr-backed server (Folders, Projects, ChatBot, Memories) migrates upstream first; that migration is a named prerequisite for deleting that server. No second executor kind; EventStore-client-only rule stands.
- (decision) Canonical catalog operation name is module.kebab-case (e.g. parties.create-party). Routing wire values come from ICommandContract/IQueryContract statics when implemented, else from the attribute's explicit CommandType/Domain. Reason: matches Tenants and Admin CLI style; removes per-module naming drift.
- (decision) Read-only mode: with --read-only, send_command is absent from the MCP tool list and CLI 'send' exits with an error; run_query always carries readOnlyHint=true; enforcement lives in the executor, not only in hints. Reason: MCP annotations are per-tool and untrusted.
- (event) Fast path: consolidated question answered; drafting full PRD with [ASSUMPTION] tags next.
- (change) Full PRD draft written to prd.md: 12 sections, FR-1..FR-22, NFR-1..NFR-8, SM-1..SM-6 plus SM-C1..C4, 7 open questions, 17 indexed assumptions. Invented section 4.8 for legacy replacement (D6).
- (event) Finalize step 1 memlog audit: all entries captured (decisions in PRD 4.2/4.7/4.8/9.1/12; process decisions set aside). Research facts and reference-survey surprises appended to addendum C and D.
- (event) Reconcile brief.md: gaps = 'agent is a peer of the UI' premise dropped from Vision; anticipatory-build/why-now framing dropped; XML-doc-comments-vanish-at-runtime rationale dropped; 'serve' verb silently renamed 'mcp'. Contradictions: OAuth 'parked' in brief vs 'never' in PRD; Folders prerequisite is a deliberate divergence. Fixes queued until all reviewers report.
- (event) Reconcile brief addendum+memlog: gaps = query freshness/ETag (ifNoneMatch, IsNotModified) absent from run_query args and response mapping; GetCommandStatusAsync has no tool/verb; module-layout open item silently resolved (restore to Open Questions); .Cli companion count inconsistent (six vs five vs four-or-five); SDK notes (dynamic tool registration, no first-class per-mode tool filtering) missing from addendum B. Contradiction: no-new-server rule effective date must be 2026-09-21 (brief decision date), not 'PRD acceptance'.
- (event) Rubric review: no critical; 5 high = FR-7 hiding tenant-like props breaks Tenants (AggregateId => TenantId); idempotentHint contradicts per-call key + caller retries; no rule for what counts as an identifier (Projects uses typed ProjectId); Decoration Package home/feed and publish-first ordering undecided; overlength via restatement (~1,200-1,500 words cuttable). 6 medium, 5 low in review-rubric.md.
- (decision) Idempotency: send_command and CLI send accept an optional caller idempotency key (ULID); tool generates one when absent and returns it in the result; tool never retries; idempotentHint=false. Reason: resolves the contradiction between per-call keys and caller retries (reviewer finding 0.3).
- (decision) Payload/envelope overlap (closes OQ-3 and OQ-2): Schema includes every declared property; attribute may name tenantProperty, correlationProperty, idempotencyKeyProperty; executor fills them from the envelope before submission and rejects a payload whose tenant differs; task/actor context stays ordinary payload. No breaking change for Tenants or Projects.
- (decision) v1 gate: v1 is done when the tool is released and Tenants and Parties are covered end to end; Projects and Folders follow as they become Gateway-ready; SM-1 checked at three months for all four. Reason: Folders rewrite must not be the critical path.
- (decision) No-new-server rule covers per-module agent CLIs: no new ones; the five existing (Folders, Projects, ChatBot, FrontComposer, Memories) are frozen to bug fixes and listed as deletion candidates on the same parity bar. Effective date is 2026-09-21, the brief decision date.
- (decision) Autofix decisions from reviewer gate: Decoration Package lives in this repo and is published before upstream decoration begins; query routing gains projectionActorType (attribute-only) and the aggregate-id chain applies to queries; chain order = explicit argument > attribute-named property > interface via JSON path, conflict = validation_failed; identifier rule = known identifier type or name ending in Id, overridable; v1 query args = paging, entityId, extensions; search/filters/orderBy/freshness deferred and excluded from parity; tenant default stored in ~/.eventstore/mcpcli.json keyed by profile name (admin CLI would drop an added field); exit codes: 0 result, 1 result plus diagnostics for discovery verbs only, 2 no result; describe --strict renamed --lint; glossary Read-only Mode = write submission refused, discovery unchanged; parity = tools/resources whose effect is a gateway command/query, stream/file resources dropped; migration plan is a named v1 deliverable; test fixture Contracts library allowed in tests; OAuth softened to parked (not v1 or next); 'serve' renamed 'mcp' acknowledged; Legacy Server count corrected to four standalone plus FrontComposer host with Projects plug-in; global-admin Tenants commands not decorated in v1; module layout restored as open question; command status lookup deferred; SM-7 first-party adoption added.
- (event) Finalize step 3 reviewer gate: rubric review (0 critical, 5 high) and downstream review (6 critical, 18 high) triaged; all critical/high either autofixed or decided by user; medium/low applied where cheap during rewrite. Step 4 open-item triage folded into the same rewrite.
- (change) prd.md rewritten after reviewer gate and reconciliation: attribute member table; per-field routing precedence; schema includes all properties with readOnly envelope-filled members; identifier rule; aggregate-id chain for commands and queries; caller idempotency key; exit-code table; read-only glossary fix; parity definition and migration plan deliverable; v1 gate; Gateway-ready checklist with per-module prerequisites; CLI freeze rule dated 2026-09-21; SM-7; OQs re-cut to 7 (module layout restored; OQ-2/OQ-3 closed by decision). addendum.md gained §E argument table, env vars, profile files and §F deferred capabilities; SDK notes; FrontComposer as prior generic host.
- (change) Polish applied to addendum.md: structure rows 1-3 (cross-section pointers, landscape bullet split, uniform headings; row 4 preserved) and all six prose rows (semicolon fix, relative clause, D6 antecedent, US spelling, plug-in, URL default wording).
- (change) Polish applied to prd.md: structure rows 2-11 and 16 accepted (constraints moved before features, vision leads with the build, non-goals merged into scope, assumptions index condensed to pointers, journeys condensed, read-only merged into executor section); row 1 partially (glossary kept in place per template, 11 entries condensed to pointers); row 15 rejected (testable consequences stay in the PRD as story input); all 20 prose rows plus the 11 minor ones applied (US spelling, Frozen CLI term, FR-6 conflicting-value category, FR-16 check-before-overwrite, FR-19 active voice). Final length 5,725 words, above the 2,500-4,000 target; remaining lever is the rejected row 15.
- (event) Finalize step 6 external handoffs: none configured, skipped.
- (event) PRD finalized
- (event) Update intent: amend PRD in place from architecture spine 'PRD Amendments' (architecture-mcpcli-2026-09-22/ARCHITECTURE-SPINE.md); FR ids kept stable; no conflicts with prior decisions found, every amendment refines an existing rule.
- (change) FR-7 and §3 (AD-8): Identifier is now a property marked with the identifier attribute or named by aggregateIdProperty, never inferred from an Id suffix; typed string with ULID pattern only when the Module's Identifier Kind is Ulid; value-object identifiers serialize per their own converter; non-string identifier excludes the Operation (invalid identifier type); unmarked *Id property is a plain string plus a lint warning. Reason: Tenants tenant id 'system' is not a ULID.
- (change) §4 Identifiers guardrail (AD-8): 'Identifiers are ULIDs' narrowed to generated and caller-supplied envelope identifiers; Payload identifiers follow the Module's Identifier Kind; Tenant is a non-empty string, never ULID-validated. New glossary term Identifier Kind.
- (change) FR-3 (AD-8): Module marker gains Identifier Kind (Ulid or String); FR-15 validates the aggregate identifier by that kind (Ulid.TryParse for Ulid, non-empty for String).
- (change) §5.1 table and FR-2 (AD-19): aggregateIdProperty required only for attribute-routed Commands; optional for Queries and for interface-routed Commands, whose aggregate identifier is read through the contract getter.
- (change) FR-16 (AD-19): 'JSON path derived once at startup' replaced by 'accessor compiled once at startup'; a Command with no aggregate-id source is excluded at Catalog build (FR-2), disagreement checked against the accessor value; OQ-3 assumption on aggregate-less Queries kept.
- (change) FR-20, §4 and §8.1 (AD-15): v1 references only Contracts packages satisfying the closure rule (no other Hexalith package, no framework reference beyond the base runtime, CI test enforces); today Tenants and Parties; slimming Hexalith.Projects.Contracts added as a Projects Gateway-ready prerequisite. v1 gate unchanged.
- (change) FR-6 and FR-14 (AD-3): an empty Catalog fails only discovery verbs, send, query and mcp with exit 2 catalog_empty; config and --version never build the Catalog. FR-6 error categories gain invalid identifier type.
- (change) FR-13 (AD-13): --strict added to the global option list with EVENTSTORE_STRICT; addendum §E environment variable list updated.
- (change) FR-4: describe --lint scope extended to unmarked identifier-like properties so the FR-7 lint warning has an owning requirement (spine lint convention unmarked_identifier).
- (decision) OQ-2 resolved, source architecture spine 2026-09-22: flat layout src/Hexalith.McpCli.* and tests/Hexalith.McpCli.*.Tests; addendum §B pointer updated.
- (decision) OQ-4 resolved, source architecture spine 2026-09-22 (AD-18): analyzer is a separate project packed into the Decoration Package, separable story, package may ship before it; FR-1 assumption tag removed and §11 index updated.
- (decision) OQ-7 resolved, source architecture spine 2026-09-22 (AD-17): command hexalith; packages Hexalith.McpCli (tool) and Hexalith.McpCli.Abstractions (Decoration Package) sharing one version; §3 and §7 name them.
- (change) Addendum §D: stale pointer 'deletion targets is PRD Open Question 4' corrected; Frozen CLIs are governed by FR-22 and the FR-21 parity bar (decided at the reviewer gate, pointer never updated).
- (event) PRD amendment pass complete; status stays final, updated 2026-09-22; OQ numbering preserved, OQ-1/3/5/6 remain open.
- (event) Validate run 2026-09-22 (rubric + adversarial + spine-alignment): grade Poor, blocker-driven; 4 verified criticals (OQ-3 empty query AggregateId rejected by Gateway; Tenants fixed 'system' Envelope tenant; closure rule excludes v1 Contracts via Hexalith.Commons.UniqueIds; caller-controlled tenant/actor/extensions); report at validation-report.md/.html; PRD unchanged
- (event) Update intent 2026-09-22: change signal is validation-report.md (grade Poor, 4 critical, 14 high, 17 medium, 12 low); all findings applied unless listed as deferred; four prior decisions overturned by user choice below.
- (decision) Tenants stays a v1 Module: Module marker gains fixedTenant (Tenants = system), surfaced by describe_operation and used as Envelope default; a differing per-call tenant is validation_failed; §2.2 non-user exclusion narrowed to EventStore infrastructure administration, admin-token overlap accepted. Reason: only Tenants and Parties pass the closure rule; swapping would make v1 Parties-only.
- (decision) Caller-controlled identity closed (overturns 'per-call tenant wins'): per-call tenant honored only when the Profile has no default or allowTenantOverride is set, else validation_failed; new actorProperty attribute member filled by the executor from the resolved Actor (flag/env/Profile, never per-call) and marked readOnly; extension keys limited to a per-Profile allowlist, default empty. Reason: LLM must not choose tenant, actor, or trusted extension keys.
- (decision) Profile store (overturns shared profiles.json): ~/.eventstore/mcpcli.json is the sole store with url, token, format, tenant, actor, allowTenantOverride, allowedExtensions per profile; same file shape and config verbs as the admin CLI; profiles.json is not read. Reason: one profile name cannot serve admin API and Gateway; csv format unsupported.
- (decision) Upstream decoration slip: v1 waits for Parties; no scope fallback; §8.1 gains a per-Module table with owner role, target version and earliest pinnable release, dates open (new OQ-8, owner product owner).
- (decision) OQ-3 resolved from source 2026-09-22: Gateway SubmitQueryRequestValidator rejects an empty AggregateId and Parties sends the constant 'parties'; Query attribute gains aggregateId constant (mutually exclusive with aggregateIdProperty); chain = explicit argument > accessor > constant, nothing = validation_failed, empty never sent; §8.1 gains a list-tenants query spike before the executor epic.
- (change) Dependency policy rewritten as an allowlist (§4): *.Contracts packages + Hexalith.EventStore.Contracts closure at pinned version (Hexalith.Commons.UniqueIds, ByteAether.Ulid) + no FrameworkReference beyond Microsoft.NETCore.App; additions are a PRD change; SM-C3 extended. Reason: old 'no other Hexalith package' rule excluded Tenants and Parties themselves. Spine AD-15 needs the same fix (handoff to architecture).
- (change) Module marker table added to §5.1: name, description, identifierKind, fixedTenant, wireTypeConvention (Explicit|FullTypeName|KebabCase), serializerOptionsProvider. Decoration Attribute gains aggregateId (query constant) and actorProperty; projectionActorType now optional (Tenants has no projection actor); tenantProperty may never name the aggregate identifier source (FR-6 error).
- (change) FR-6/FR-14 exit-code contract narrowed: exit 1 only for describe --lint findings; Catalog diagnostics are stderr-only and never change exit codes unless --strict (exit 2 catalog_invalid before any result); property-level findings (undescribed, unmarked identifier, paging members) are lint-only. Reason: zero [Description] attributes exist today, so discovery verbs would exit 1 for the whole migration.
- (change) FR-16 rewritten: tenant chain (fixedTenant > gated per-call > flag/env/Profile, none = validation_failed); Actor required when actorProperty declared; extensions Commands-only with Profile allowlist; Queries carry no correlation id or extensions (client cannot); paging only via run_query arguments (spine AD-20); get-only members excluded from Schema (AD-7). FR-15 pre-validates Envelope fields against Gateway patterns.
- (change) Other findings applied: FR-1 analyzer deps private assets; FR-7 Ulid recognized by full name ByteAether.Ulid.Ulid; FR-5 HexalithContracts=true PackageReference metadata; FR-9 three label names (Purpose/Use when/Next); FR-11 gateway_error code and edit-distance suggestions; FR-12 table format fallback + unsupported_transport before Catalog; FR-13 --profile first, --actor, --allow-tenant-override, boolean env parsing; FR-19 split into head and executor guarantees; FR-20 'covered end to end' defined, empty_module vs invisible; FR-22 closes on PR opened; NFR-1 ubuntu-latest, NFR-2 outputSchema measured separately, NFR-6 Dapr note, NFR-7 tag replaced by AD-16 pointer; §7 Module obligation for name stability; §8.1 upstream schedule table + checklist additions; §8.2 typed tools parked, HTTP only committed next release; SM-1/SM-4/SM-5/SM-6 made observable; UJ-1 rewritten over Tenants+Parties with dedup assumption; §2.2 admin-plane boundary; Legacy Server count fixed at six packages; OQ-8 (upstream dates) and OQ-9 (Gateway dedup) added; §11 index updated.
- (change) addendum.md rewritten: §A gains two 2026-09-22 rejections; §B Gateway validator facts, runtime tool registration (AD-12), Ulid full-name; §C duplicate block removed; §D pointers fixed (§4, §8.1), Tenants tenant/actor facts; §E tenant gating, correlationId/extensions send_command-only, --actor/--allow-tenant-override, resolution order with --profile first, mcpcli.json example and config set; §F pointers fixed; new §G result and error documents; new §H Projects/Folders prerequisite detail moved from PRD §8.1.
- (change) AGENTS.md/CLAUDE.md/.github/copilot-instructions.md line 85 'Identifiers are ULIDs' replaced by Envelope-ULID / Identifier-Kind rule; sync script passes.
- (assumption) Deferred as accepted: PRD length stays above the 2,500-4,000 target (about 7,700 words) because testable consequences stay in the PRD per the 2026-09-21 polish decision; Projects/Folders detail moved to addendum §H. Owner: product owner; revisit if a downstream reader reports the length as a blocker.
- (event) Verification pass (verify-update-2026-09-22.md): 44/47 findings resolved, 2 partial, 1 accepted (length); 11 contradictions and 11 ambiguities fixed in place.
- (decision) CLI has no per-call tenant: --tenant is a session setting; the FR-16 override gate applies only to the MCP tenant argument. URL has no default (missing URL = configuration_invalid) instead of an unnamed 'Gateway default'. Sample Module count is one. Reason: verifier found the gate undecidable on the CLI and the default URL named nowhere.
- (event) Handoff to architecture recorded in PRD §10: spine AD-7, AD-9, AD-11, AD-15, AD-19 must be amended before the executor epic.
- (change) AGENTS.md/CLAUDE.md/copilot-instructions: dependency line rewritten as the allowlist, tenant line gains fixed tenant and operator gate, CLI-shape line names mcpcli.json; sync script passes.
- (change) Polish (bmad-review structure+prose) applied to addendum.md: 10 of 13 structure rows (§F/§G reorder and lint-row move declined, they break PRD section references); 24 of 25 prose rows (spaced em dashes kept for consistency with the PRD glossary); Folders.Contracts description aligned between §D and §H.
- (change) Polish (bmad-review structure+prose) applied to prd.md: 14 of 15 structure rows (status block after §1, §1 problem list, glossary clusters, §5 preface and FR index, FR-4/FR-14 layout, FR-6 category table, FR-16 ordered lists, §8.1 sub-headings and numbered checklist, §10 handoff/open/resolved split, §11 table, PM notes to plain text, §11 history sentence cut, sentence-case headings); FR-18/FR-19 numbers kept stable with a note because the spine references them. All 26 prose rows applied; 'accepted against' changed to 'specified against' in both files.
- (event) PRD update pass complete 2026-09-22: status stays final, updated 2026-09-22; prd.md about 8,550 words, addendum.md about 2,350; OQ-1/5/6/8/9 open; spine handoff in §10.1.
