# Reconciliation — validation report 2026-09-22 0901

Source: `validation-2026-09-22-0901/validation-report.md`  
Compared with: latest `prd.md`, `addendum.md`, and `.memlog.md`  
Verdict: all ten validation findings are resolved in the PRD/addendum contract or converted into a durable owner-gated obligation. The remaining work is downstream: architecture alignment, production of the versioned Parties inventory, and upstream Module readiness.

| Finding | Disposition | Latest reconciliation evidence | Remaining obligation |
|---|---|---|---|
| C1 — Decoration Package excluded by allowlist | Resolved in the PRD/addendum; architecture alignment pending | PRD §4 separates this repository's Decoration Package from the exact external Hexalith/domain allowlist, permits exposed Contracts Libraries to reference the dependency-free package, and keeps additions to the external set under PRD change control. The memlog records this as the accepted dependency-policy correction. | Before the dependency-closure test or Module pins, architecture AD-15 and the test must adopt the same distinction (PRD §10.1). |
| C2 — architecture records cannot emit normative public results | Resolved in the PRD/addendum; architecture alignment pending | PRD FR-11 makes addendum §G exhaustive. Section G now defines names, nesting, types, value constraints, required/optional members, omission rules, optional Gateway result payloads, accepted status semantics, and error shapes. The advanced idempotency decision removes `duplicate`, makes `idempotencyKey` caller-supplied and optional, and binds `messageId` to the Gateway's canonical execution identifier. | Before either Head or its output schemas, AD-5/12 must conform to the latest §G records (PRD §10.1). |
| C3 — Tenants example casing conflicts with Module serialization | Resolved in the PRD/addendum; architecture alignment pending | Addendum §G distinguishes canonical camel-case outer fields from Module-preserved embedded `schema`/`example` fields and uses `TenantId` in both the schema and example. | Before Schema/Catalog implementation, AD-6/7 must preserve Module serializer casing (PRD §10.1). |
| R1 — unmarked `*Id` rule contradicts schema derivation | Resolved; architecture alignment pending | PRD FR-7 preserves the ordinary serializer-derived schema and emits lint only; a suffix never types or rewrites the property. The memlog explicitly supersedes the old plain-string clause. | Before Schema/Catalog implementation, AD-8 must adopt the same rule (PRD §10.1). |
| R2 — missing-URL applicability undefined | Resolved; architecture alignment pending | PRD FR-13 and addendum §§E/G require a URL only when execution reaches the Gateway. Catalog discovery, configuration, version reporting, MCP startup, and MCP discovery remain offline-capable. `describe_operation.submittable` now reports head-level surface availability from Read-only Mode and resolved URL without attempting Tenant, actor, Payload, or per-call Envelope validation. | Before settings resolution, AD-13 must adopt execution-only URL validation (PRD §10.1). |
| R3 — Parties command subset tentative | Resolved as a durable, testable upstream gate | FR-21 makes the versioned migration-plan inventory the scope authority: each legacy operation has an approved include/exclude row, rationale, decorated type when included, version, and maintainer approval. FR-20 compares that inventory with `list_operations parties`; missing, extra, or unapproved rows fail Gateway readiness. The inventory never becomes runtime configuration. | The Parties maintainer must approve and version the actual inventory before decoration, then pass the inventory-to-Catalog check. This is an intentional delivery obligation, not a remaining product-rule ambiguity. |
| R4 — lint outcome absent from normative result | Resolved; architecture alignment pending | PRD FR-4/FR-9 and addendum §§E/G make `lintFindings` always present and `[]` when empty. `code`, `severity`, and `message` are required; `property` is an RFC 6901 pointer required for recursive property-level findings and omitted for the later-added operation-level `hollow_description`. The memlog records this reviewer refinement after the original four-field decision. | Before either Head or its output schemas, AD-5/12 must include the refined lint contract (PRD §10.1). |
| C4 — SM-1 could pass without Folders | Resolved | PRD §1 and §8.1 say Projects and Folders both gate SM-1; SM-1 names Tenants, Parties, Projects, and Folders while the v1 release gate remains Tenants and Parties. The schedule now marks both follow-on Modules as SM-1 gates. | Projects and Folders still have to become Gateway-ready and be added by package reference plus rebuild before the three-month metric is assessed. |
| C5 — two synthetic Modules violate the one-Module fixture boundary | Resolved in the PRD/addendum; architecture alignment pending | PRD NFR-1 fixes the production-like Catalog and benchmark at one declared synthetic sample Module; the other Identifier Kind is tested through isolated construction. The memlog records the one-Module decision. | Before the fixture story, the architecture test conventions must adopt the same boundary (PRD §10.1). |
| C6 — handoff lists already-completed amendments | Resolved | PRD §10.1 marks the earlier AD-7/9/11/15/19 handoff complete, links `verify-update-2026-09-22.md`, and replaces the stale list with implementation-gated current obligations. Unaffected Catalog preparation may continue. | Complete the current §10.1 gates; no stale amendment remains represented as pending. |

## Qualitative gap check

No qualitative product idea from the validation report was lost. The amendments preserve offline discovery, the four-Module SM-1 target, one production-like synthetic Module, Module-owned Payload casing, optional-field omission, and the distinction between work that may continue and contracts that gate their owning implementation stories.

The later advanced-elicitation decision deliberately supersedes—not silently drops—the earlier generic safe-retry and duplicate-result assumption: v1 generates only the message identifier, accepts and echoes an idempotency key only when the caller supplies one, never retries, exposes no `duplicate` field, and warns that logical retry safety depends on a trusted per-Command Gateway adapter. OQ-9 is now resolved from pinned EventStore source. Reviewer autofixes add exhaustive public types and constraints, recursive lint pointers, head-level `submittable`, strict-MCP channel behavior, FR-22 merge-based completion, and story-aligned architecture gates without reversing any C1–C6/R1–R4 resolution.

The Parties scope rule is now durable rather than conversational: FR-21 owns the versioned inventory and approval evidence, FR-20 owns the comparison with the built Catalog, and §8.1 makes both prerequisites to Gateway readiness.

## New inconsistency check

No new inconsistency was found among the latest PRD, addendum, and memlog. Older append-only memlog entries describing generated idempotency keys or a possible duplicate result are historical decisions explicitly superseded by later decision lines and by current UJ-1, §4, FR-16, FR-17, addendum §§E/G, and resolved OQ-9.

## Remaining downstream obligations

- Align the architecture spine at the six implementation gates in PRD §10.1: fixture boundary, dependency allowlist, serializer casing and unmarked-identifier schema behavior, execution-only URL validation, caller-supplied-only idempotency, and exhaustive public output records.
- Produce, approve, version, and verify the Parties FR-21 inventory before Parties decoration and v1 coverage acceptance.
- Complete the existing upstream readiness commitments: Tenants and Parties gate v1; Projects and Folders gate SM-1.
